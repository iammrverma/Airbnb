<% layout("/layouts/boilerplate")%><%=success%>
<div class="row">
  <div class="col-6 offset-3">
    <h1>Login</h1>
    <form
      action="/login"
      method="post"
      class="needs-validation"
      id="signupForm"
      novalidate
    >
      <div class="form-group">
        <lable for="username" class="form-lable">Username</lable>
        <input
          name="username"
          id="username"
          type="text"
          class="form-control"
          required
        />
        <div class="invalid-feedback">Enter username first</div>
      </div>

      <div class="form-group">
        <lable for="password" class="form-lable">Password</lable>
        <input
          name="password"
          id="password"
          type="password"
          class="form-control"
          required
        />
        <div class="invalid-feedback">Enter password first</div>
      </div>

      <button class="btn btn-dark">Login</button>
    </form>
  </div>
</div>
